#include <bits/stdc++.h>

using namespace std;

typedef pair<int,int> ii;

int main(){
	int n,m,k;
	//n = number bricks, m= number of colors
	// k = its color differs from the color to the left
	cin >> n >> m >> k;
	vector<int> bricks(n);
	for(){
		for(){
			for(){
				
			}
		}
	}
	
	return 0;
}
